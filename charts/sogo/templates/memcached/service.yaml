{{- /*
SPDX-License-Identifier: APACHE-2.0
*/}}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "common.names.fullname" . }}-memcached
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" . | nindent 4 }}
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
  {{- if or .Values.commonAnnotations .Values.memcached.service.annotations }}
  annotations:
    {{- if .Values.commonAnnotations }}
    {{- include "common.tplvalues.render" (dict "value" .Values.commonAnnotations "context" $) | nindent 4 }}
    {{- end }}
    {{- if .Values.memcached.service.annotations }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.memcached.service.annotations "context" $) | nindent 4 }}
    {{- end }}
  {{- end }}
spec:
  type: {{ .Values.memcached.service.type }}
  {{- if .Values.memcached.service.sessionAffinity }}
  sessionAffinity: {{ .Values.memcached.service.sessionAffinity }}
  {{- end }}
  {{- if .Values.memcached.service.sessionAffinityConfig }}
  sessionAffinityConfig: {{- include "common.tplvalues.render" (dict "value" .Values.memcached.service.sessionAffinityConfig "context" $) | nindent 4 }}
  {{- end }}
  {{- if and .Values.memcached.service.clusterIP (eq .Values.memcached.service.type "ClusterIP") }}
  clusterIP: {{ .Values.memcached.service.clusterIP }}
  {{- end }}
  {{- if (and (eq .Values.memcached.service.type "LoadBalancer") (not (empty .Values.memcached.service.loadBalancerIP))) }}
  loadBalancerIP: {{ .Values.memcached.service.loadBalancerIP }}
  {{- end }}
  {{- if and (eq .Values.memcached.service.type "LoadBalancer") .Values.memcached.service.loadBalancerSourceRanges }}
  loadBalancerSourceRanges: {{- toYaml .Values.memcached.service.loadBalancerSourceRanges | nindent 4 }}
  {{- end }}
  {{- if or (eq .Values.memcached.service.type "LoadBalancer") (eq .Values.memcached.service.type "NodePort") }}
  externalTrafficPolicy: {{ .Values.memcached.service.externalTrafficPolicy | quote }}
  {{- end }}
  ports:
    - name: memcached
      port: {{ .Values.memcached.service.ports.http }}
      targetPort: memcached
      {{- if and (or (eq .Values.memcached.service.type "NodePort") (eq .Values.memcached.service.type "LoadBalancer")) (not (empty .Values.memcached.service.nodePorts.http)) }}
      nodePort: {{ .Values.memcached.service.nodePorts.http }}
      {{- else if eq .Values.memcached.service.type "ClusterIP" }}
      nodePort: null
      {{- end }}
    {{- if .Values.memcached.service.extraPorts }}
    {{- include "common.tplvalues.render" (dict "value" .Values.memcached.service.extraPorts "context" $) | nindent 4 }}
    {{- end }}
  selector: {{- include "common.labels.matchLabels" . | nindent 4 }}